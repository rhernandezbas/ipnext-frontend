// #85 — Archived tickets view: queries `archived: true` and shows "Tickets Archivados" title.
import TicketsListPage from './TicketsListPage';

export default function TicketsArchivedPage() {
  return <TicketsListPage archivedView />;
}
